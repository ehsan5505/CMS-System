<% @page_title = "Edit #{@section.name}" %>

  <%= link_to("<< Back to Index",{:action=>"index",:pg=>@page.id,:sub=>@subject.id},:class=>"btn btn-default btn-xs  pull-right") %>
    <h2> Editing Section </h2>
	<%= form_for(:section,:url=>{:action=>'update',:id=>@section.id,:pg=>@page.id,:sub=>@subject.id},:role=>"form",:class=>"form-horizontal") do |f| %>
	  
	  <%= render(:partial=>"form",:locals=>{:f => f}) %>
	 
	      <%= submit_tag("Update",:class=>"btn btn-primary") %>
      <% end %>